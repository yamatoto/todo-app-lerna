{
  "name": "todo-app-lerna",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix packages/api-server\" \"npm run dev --prefix packages/app\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "packages/app",
    "packages/api-server"
  ],
  "devDependencies": {
    "concurrently": "^8.2.2",
    "lerna": "^8.0.0"
  }
}
